# Production environment variables for Vite
# Set this to the real backend URL when the API is hosted separately.
# For same-origin deployments, keeping it relative avoids broken DNS fallbacks.

VITE_API_URL=/api
